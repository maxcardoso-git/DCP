server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html/dcp;
    autoindex on;

    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods "GET, OPTIONS";
    add_header Access-Control-Allow-Headers *;

    location / {
        try_files $uri $uri/ =404;
    }
}
